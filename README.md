# local-process-recovery-figures

Python script for generating figures used in the evaluation of a local process recovery method.

## Repository contents

- `local_process_recovery_bw_figures_v2.py` — script for generating the article figures

## Notes

The generated figures, source datasets, and archived research outputs are planned to be published separately in Zenodo.

## Usage

```bash
python "local_process_recovery_bw_figures_v2.py"
